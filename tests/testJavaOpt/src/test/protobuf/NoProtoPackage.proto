syntax = "proto2";

option java_package = "com.turbonomic.protoc.pojo.gen.nopackage";
option java_outer_classname = "NoProtoPackage";

// A random message
message ASDF {
  // Some field
  required string field = 1;
}

// A parent message
message JKL {
  // The source.
  optional ASDF source = 5;
}
